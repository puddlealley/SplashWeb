{
  "name": "database",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "migrate": "prisma2 migrate save --experimental",
    "deploy": "prisma2 migrate up --experimental && prisma2 generate"
  },
  "dependencies": {
    "@prisma/client": "^2.26.0",
    "graphql-yoga": "^1.18.3",
    "nexus": "^0.12.0",
    "nexus-prisma": "^0.10.0"
  },
  "devDependencies": {
    "prisma2": "2.0.0-preview025",
    "dotenv-cli": "^3.1.0"
  }
}